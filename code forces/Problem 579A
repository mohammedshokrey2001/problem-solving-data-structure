#include<iostream>
using namespace std;
int main()
{
    int num, count = 0;
    cin >> num;
 
    while (num / 2 != 0)
    {
        if (num % 2 == 1)
            count++;
 
        num = num / 2;
    }
 
    cout << count + 1;
 
    return 0;
}
