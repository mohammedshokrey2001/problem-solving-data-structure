#include<cmath>
#include <iostream>
using namespace std;
 
int main() {
	int n;
	int a, b;
	int cap = 0;
	int stil = 0;
	cin >> n;
	for (int i = 0; i < n; i++) {
		
		cin >> a >> b;
		stil -= a;
		stil += b;
		cap = max(cap, stil);
 
	}
	cout << cap;
}
